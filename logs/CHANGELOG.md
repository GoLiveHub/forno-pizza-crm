# Журнал работ — Forno Pizza CRM (`pizzacrm`)

> Журнал ведётся по правилам ТЗ (раздел 9). Диффы изменений — в `logs/diffs/`.

## 2026-08-12 — Полировка русского перевода (UI + серверные ошибки)

**Добиты непереведённые UI-строки (`js/i18n.js`, `site/js/i18n.js`):**
- `js/modules/menu.js`: «(тісто)»→«(тесто)», «Добавок немає. Вкажіть ціну добавки у «Склад».»→«Добавок нет. Укажите цену добавки в «Склад».», «доступні за доплату»→«доступны за доплату», «можна прибрати з позиції (безкоштовно)»→«можно убрать из позиции (бесплатно)», «Фото (показується на вітрині)»→«Фото (показывается на витрине)», «Фото більше 4 МБ»→«Фото больше 4 МБ», «Не вдалося зберегти позицію»→«Не удалось сохранить позицию»; частичные «Кількість (»→«Количество (», «тісто»→«тесто».
- `js/modules/settings.js`: «Збережено. Оновлюємо ціни…»→«Сохранено. Обновляем цены…».
- Витрина `site/js/app.js`: «немає в наявності»→«нет в наличии», «готівка»→«наличные», «картка при видачі»→«карта при выдаче».

**Полный перевод серверных ошибок PHP (показываются в toast на русской версии):**
- ~75 пар в `js/i18n.js` (+~10 в `site/js/i18n.js`) для `api/` всех модулей: auth, chat, clients, discounts, helpers (лимит спроб), kds, menu (категории/позиции/топпинги/фото), orders (статусы/курьеры/валидация), orders_core (промокоды, «недоступна (немає: …)», адрес, блокировка клиента), order_in, pos, reports, settings, stock, users. Динамические фразы — через PARTIALS («недоступна (немає: », «Мінімальна сума замовлення: »).
- Механизм: observer уже переводит вставленные toast/DOM-узлы, поэтому серверные строки достаточно положить в словарь.
- Версии: `app.html` (i18n v19), `site/index.html` (i18n v17).
- Проверка: node-скрипты `full_i18n_scan.js` (CRM — остались только артефакты regex на апострофах «Кур'єр», SITE — 0 остатков) и `check_server_msgs.js` (серверные сообщения — 0 реальных остатков).

## 2026-08-09 — Исправления по жалобам: перевод, цены

**Перевод всего интерфейса на русский (словари `js/i18n.js`, `site/js/i18n.js`):**
- Курьеры: «Кур'єрів»→«Курьеров», «Вільні»→«Свободные», «Зайняті»→«Занятые», «Активних доставок»→«Активных доставок», «Вільний»→«Свободен», «Зайнятий»→«Занят», «Активні замовлення»→«Активные заказы», «Немає активних замовлень»→«Нет активных заказов».
- Категории/ингредиенты/тесто: «Піца»→«Пицца», «Напої»→«Напитки», «Десерти»→«Десерты», «Тісто 30 см»→«Тесто 30 см», «Моцарела»→«Моцарелла», «Базилік»→«Базилик», «Салямі піканте»→«Салями пиканте», «Вершки»→«Сливки», «Рукола»→«Руккола», «Чотири сири»→«Четыре сыра», «Горіхи»→«Орехи», «Рикота»→«Рикотта», «Шинка»→«Ветчина», «Гриби»→«Грибы», «Трюфельний крем»→«Трюфельный крем», «Тонке»→«Тонкое», «Пишне»→«Пышное», «Лід»→«Лёд».
- Блюда: «Діавола»→«Диавола», «Кватро Формаджі»→«Кватро Формаджи», «Тірамісу»→«Тирамису», «Апельсиновий фреш»→«Апельсиновый фреш», «пишне тісто»→«пышное тесто».
- Служебные: «Закупівля»→«Закупка», «Фіксована сума»→«Фиксированная сумма», «Відсоток %»→«Процент %», «Пошук: …» (аудит/заказы/клиенты), «Вкажіть, будь ласка, …» (ошибки формы), «Щось пішло не так…», «Клієнт у чорному списку!», «Клієнт скасував», «Написати клієнту...», «Ваше повідомлення...», «Оператор підтримки», «Інша причина», «Довге очікування», «Доставка кур'єром», «Немає інгредієнтів», «Замовлення збережено на сервері.», «2 по ціні 1», «Ім'я (не обов'язково)».
- Версии скриптов подняты: `app.html` (i18n v15, ui v6), `site/index.html` (i18n v13, app v3), `kds.html` (i18n v7), `login.html` (i18n v14).

**Цены (`js/ui.js`, `site/js/app.js`):**
- `UI.money()` больше НЕ умножает базовую цену на курс для гривны: грн = базовая цена, руб = базовая × курс. (Раньше Маргарита 180 грн показывалась как «1 530 грн» из-за умножения на 8.5.)
- Продажное округление: дробные цены округляются вверх до целого, заканчивающегося на 9 (248.1 → 249, 1293.4 → 1299, 1946.5 → 1949). Целые не трогаются.
- То же продажное округление применено на витрине (`site/js/app.js`), валюта витрины — грн.

## 2026-08-11 — Курс валют, прайс в рублях, дизайн карточки меню

**Курс:** дефолт `biz_rate` исправлен с 8.5 на **1.84** руб за 1 грн (`api/settings.php`, `js/app.js`, `js/modules/settings.js`). 58 349 грн → 107 362 руб (было 495 966 руб). Если в настройках уже сохранён старый курс — обновить его там.

**Прайс (БД `menu_items`)** пересчитан под целевые цены в рублях (пицца ≈ 1200–1400 ₽, напитки ≈ 250 ₽) при курсе 1.84:
- Маргарита 650/975, Діавола 705/1060, Прошутто 735/1100, Кватро Формаджі 705/1060, Кальцоне 650/975, Трюфель 760/1140 (грн 30см / 40см).
- Лимонад 135, Апельсиновий фреш 135, Тірамісу 155 (грн).
- Показ в руб с продажным округлением: 1199, 1299, 1359, 1399, 249, 289 ₽ (30 см).
- Ингредиент id=3 переименован в «Моцарелла» (было «Моцарела»).

**Дизайн карточки меню (`css/polish.css`, v4):**
- Иконка 🍕 увеличена с 34px до 64px, светлее (`--ink-2`) — меньше пустого фона.
- Описание поднято до 14px, в тёмной теме цвет `#dcd2c5` (контрастнее).
- Цена: `white-space: nowrap` + `letter-spacing` — «руб» не переносится, цифры не слипаются.
- Кнопка «Удалить» (`.mini-btn--danger`) теперь красная постоянно, а не только при наведении.
- Версии: `app.html` (app.js v8, settings.js v7, polish.css v4).

**Иконки и карточки меню (по жалобе):**
- Иконка товара выбирается по категории: пиццы — 🍕, напитки — 🥤, десерты — 🍰 (`catEmoji` в `js/modules/menu.js` и `site/js/app.js`). Вместо одинаковой заглушки у всех позиций.
- Карточки меню выровнены по высоте (`height:100%`), кнопки прижаты к низу (`margin-top:auto` на футере) — текст больше не прилипает к кнопкам «Изменить».
- Фильтры категорий (Пицца/Напитки/Десерты) в тёмной теме: явный светлый текст `#f0e8dd` на `#28231a` — читается.
- Кнопка «Удалить»: убран постоянный красный контур — теперь только красный текст, обводка обычная; красное выделение остаётся при наведении (меньше визуального шума).
- Версии: `app.html` (menu.js v7, polish.css v5), `site/index.html` (app.js v4).

**Верстка и UX (вторая волна):**
- «Моцарелла» исправлена во всех описаниях состава в БД (`descr` блюд: Маргарита, Диавола, Прошутто, Кальцоне, Трюфель) — REPLACE в `menu_items`.
- Кнопки «Новая категория»/«Новая позиция» — равная ширина (`flex:1`).
- Переключатель языка УКР/РУС переработан в аккуратный pill-тогл: без плавающей белой плашки и пёстрых кнопок, активный сегмент — красная заливка с белым текстом (`js/i18n.js`, `site/js/i18n.js`, dark-стили в `polish.css`).
- Контраст кнопок действий: `.mini-btn` переведён на основной цвет текста `var(--ink)` — «Изменить» читается на тёмном фоне.
- Дате в шапке задан `white-space:nowrap` + отступ.
- Версии: `app.html` (i18n v16, menu v8, polish v6), `site/index.html` (i18n v14), `kds.html` (i18n v8), `login.html` (i18n v15).

**Единая редактура состава и выравнивание (третья волна):**
- Состав блюд (`descr` в БД) переписан на русский для всех позиций: «томатный соус, моццарелла, базилик» (Маргарита), «сливки, моццарелла, прошутто, руккола» (Прошутто), «трюфельный крем, моццарелла, грибы» (Трюфель) и т.д. — вместо украинских «вершки/гриби/трюфельний/базилік».
- «Моцарелла» → «Моццарелла» (две «ц», по требованию) — в `ingredients` (id=3) и во всех составах.
- В словари (`js/i18n.js`, `site/js/i18n.js`) добавлены подстрочные замены PARTIALS для ингредиентов: «вершки»→«сливки», «томатний соус»→«томатный соус», «салямі піканте»→«салями пиканте», «рукола»→«руккола», «чотири сири»→«четыре сыра», «горіхи»→«орехи», «рикота»→«рикотта», «шинка»→«ветчина», «гриби»→«грибы», «трюфельний крем»→«трюфельный крем», «базилік»→«базилик», «мед»→«мёд», «свіжий апельсин»→«свежий апельсин», «м'ята»→«мята» (страховка от укр-ввода).
- Кнопки «Изменить/Удалить» на одной нижней линии: описание обрезается на 3 строки (`-webkit-line-clamp`) с резервной высотой `min-height:4.5em` — карточки одной высоты.
- Дате в шапке задан отступ `margin-right:8px`.
- Версии: `app.html` (i18n v17, polish v7), `site/index.html` (i18n v15), `kds.html` (i18n v9), `login.html` (i18n v16).

**Финальная правка орфографии и отступов (четвёртая волна):**
- «Моццарелла» (две «ц») возвращено к словарному написанию **«Моцарелла»** (одна «ц», две «л») — в `ingredients` (id=3) и во всех составах БД.
- Подтверждено: «мёд» и «трюфельный» уже русские в `descr` (жалобы «меде/трюфельний» относились к закешированному рендеру).
- Цена в карточке больше не липнет к кнопке «Изменить»: `.menu-card__foot` получил `gap:12px`.
- Выравнивание блоков в карточке решено ранее: описание фиксированной высоты 3 строки (`min-height:4.5em` + `-webkit-line-clamp:3`) — кнопки и цена на одной линии.
- Дата в шапке: `margin-right:8px`.
- Версии: `app.html` (i18n v18, polish v8), `site/index.html` (i18n v16), `kds.html` (i18n v10), `login.html` (i18n v17).

**Контраст кассы и UX (Accessibility):**
- Кнопка «Оформить» при пустой корзине: серый фон, тусклый текст, `cursor:not-allowed` (переопределение `.btn[disabled]`; кнопка уже была `disabled`, но краснела из-за opacity).
- Цены на плитках кассы: базовая цена крупнее (16px), цена 40 см мельче и тусклее (11px, `--ink-3`) с отступом — не перепутать.
- Поля ввода (Имя/Промокод и др.) в тёмной теме: фон `#2c2620` (не чёрный), заметная рамка `#6b6053`, читаемый placeholder.
- Левое меню: неактивные вкладки подняты до `rgba(255,255,255,.9)`.
- Плашка пользователя: роль скрывается, если совпадает с именем (дубль «Администратор/АДМИНИСТРАТОР» устранён); названия ролей переведены на русский.
- Дата в шапке: отступ `margin-right:12px`.
- «Дисерты» — опечатка в жалобе; в БД категория «Десерти», на экране «Десерты» (проверено).
- Версии: `app.html` (app.js v9, polish v9).

**Доступность кассы (вторая итерация, v10):**
- Названия товаров на плитках кассы в тёмной теме — белые, жирные (`#fff`), в светлой — основной цвет. Больше не сливаются с фоном.
- «48 см: 1 794 руб» и «шт» в тёмной теме осветлены до `#c7bbaa` — читаемо.
- Неактивные пункты левого меню подняты до `rgba(255,255,255,.95)`.
- Дата в шапке: `margin-right:20px` + `.topbar__right { padding-right:20px }` — не упирается в край.
- Дубль в профиле: роль скрывается, если имя равно названию любой роли (рус/укр, в т.ч. регистр) — «Администратор/АДМИНИСТРАТОР» исчезает.
- Поле «Промокод»: отступ слева уже есть (`padding: 9px 12px` из base.css) — на свежем скрине виден.
- Версии: `app.html` (app.js v10, polish v10).

**Управление витриной из CRM: фото позиций меню:**
- CRM «Меню і прайс»: у позиции добавлено поле «Фото» — превью, кнопки «Завантажити фото» / «Прибрати» (`js/modules/menu.js` v9). Файл загружается в `uploads/menu/` (до 4 МБ, png/jpg/webp/gif), путь хранится в `menu_items.img`.
- Backend: новый action `upload_image` в `api/menu.php`, `save_item` сохраняет `img` (с валидацией пути).
- Витрина `site/`: карточки показывают фото (`<img src="../uploads/menu/...">`), если оно задано, иначе эмодзи по категории; `api/order_in.php?menu=1` отдаёт `m.img` (`site/js/app.js` v5, `site/css/style.css`).
- Защита `uploads/.htaccess` (запрет исполнения PHP, скрытие листинга).
- Протестировано end-to-end: login → upload_image → save_item → витрина отдаёт img → delete_item (тестовая позиция удалена).
- Версии: `app.html` (menu.js v9, polish v11), `site/index.html` (app.js v5).

## 2026-08-09 — Доработка UX: кастомизация, валюта, тёмная тема, прайс-лист

**Кастомизация позиций (POS и витрина):**
- Убрана метка-бейдж «кастомізація» на плитках — клик по позиции и так открывает кастомизацию.
- Окно кастомизации переработано: шапка с названием и ценой, описание, блоки «Тісто» (только для пицц), «Розмір», «Інгредієнти (прибрати — безкоштовно)», «Добавки (додати — доплата)», итог.
- Блок «Тісто» показывается только если у позиции есть тесто/40 см (для напитков скрыт).
- Напитки стали кастомизируемыми: сид `crm_seed_drink_addons()` добавил ингредиенты **Лід (10), Лимон (12), Сироп (15)** и подключил к напиткам добавки **Мед/Лід/Лимон/Сироп** (через `menu_toppings`). «Апельсиновий фреш» в кастомизации: состав (можно убрать) + 4 добавки.

**Валюта (грн/руб):**
- `UI.money()` теперь конвертирует по `window.CURR` (код валюты из localStorage, курс — из настроек).
- Переключатель валюты в шапке CRM (грн/руб).
- Настройки → «Вигляд та валюта»: выбор валюты и курс рубля за 1 грн (сохраняется на сервер: `save_currency`, ключи `biz_currency`/`biz_rate`).

**Тёмная тема:**
- `html[data-theme="dark"]` переопределяет палитру (`tokens.css`-переменные) в `polish.css`.
- Переключатель темы в шапке (Світла/Темна/Як у системі) + в настройках; сохраняется в localStorage.

**Курьеры:** убраны дублирующий заголовок страницы и подсказка — осталась кнопка «Оновити».

**Прайс-лист («Меню і прайс»):** редактирование позиции теперь включает блоки «Інгредієнти (можна прибрати)» и «Добавки (з доплатою)» — чекбоксы состава + цены добавок; сохранение через новое действие API `save_compose` (заменяет `recipes` и `menu_toppings`).

**Тесты:** smoke 118/118 ALL OK; CDP-пробы `ui2_probe.js` (POS/меню/валюта/тема/настройки) и `site2_probe.js` (витрина) — ALL PASS.

## 2026-08-09 — День 2 — Этап 12: полировка UI/UX, анимации, адаптив, бэкап

**Новый файл `css/polish.css`** (подключён в `app.html`, `login.html`, `kds.html` как `?v=1`; существующие стили не менялись — ТЗ 7.3):
- Адаптив: на ≤700px сетки `.grid--2/.grid--3`, `.dash-cols`, `.couriers`, `.chat-window`, `.pos__grid` складываются в 1–2 колонки; `.page-head`/`.toolbar` вертикалятся; футер модалок — колонкой на всю ширину; тосты — снизу на всю ширину; полосы отчётов `.bar-row` перестраиваются.
- Анимации: плавные hover/тень для `.card`, `.order-card`, `.stat-card`, `.menu-card`, `.stock-card`, `.pos__tile`; лёгкий «влёт» карточек `.card-in`; поддержка `prefers-reduced-motion`.
- UX: стилизация скроллбаров, focus-ринг только с клавиатуры (`:focus-visible`), `.table-wrap` для горизонтальной прокрутки таблиц.
- KDS: адаптив 1-колонки на ≤700px + крупные тач-кнопки (44px). Тёмная тема KDS уже была (`kds.css`).

**Исправление:** заголовок дашборда вернулся к `Середня тривалість приготування` (был «Середній час приготування», из-за частичной замены «час»→«година» в UA-режиме искажался до «Середній година приготування»). Пара в `i18n.js` обновлена соответственно.

**Тесты:**
- `tests/smoke.php` — 118 проверок, **ALL OK** (регрессия этапов 3/4/6/7/8/9).
- CDP-проба `etap12_probe.js` (headless Chrome, pizzarcm.local): desktop 1440×900 и mobile 390×844 — нет горизонтального переполнения (`sw<=cw`), дашборд рендерится, сайдбар открывается бургером на мобильном, KDS тёмная тема `rgb(30,30,30)`, на desktop KDS 3 колонки, на мобильном 1. **ALL OK.**

**Резервная копия:** `backup/backup_2026-08-09/` — 74 файла, включая `data/crm.db` (~788 KB).

## 2026-08-09 — День 2 — Этап 9: дашборд и отчёты

**Backend (`api/dashboard.php` — новый):**
- GET /api/dashboard.php (admin/cashier/manager/owner): выручка и число заказов сегодня/неделя/месяц (`done`), активные заказы по статусам (`new`/`cooking`/`delivering`), заказы по часам за 30 дней, ТОП-5 блюд (по qty, 30 дней, done), доставка/самовывоз/на місці (количество + выручка), среднее время готовки (из audit: `cooking → done`, fallback `closed - created`), эффективность курьеров (кол-во done-доставок + среднее время `delivering_at → closed_at`).

**Frontend:**
- Новый модуль `js/modules/dashboard.js` (group `analytics`, роли admin/cashier/manager/owner): карточки метрик (`.stat-card`), блоки «Активні замовлення», «Середній час приготування», «ТОП-5 страв», «Доставка / самовивіз», «Години пік (30 днів)», «Ефективність кур'єрів», кнопка «Оновити». Стили в новом `css/dashboard.css`.
- `app.html`: подключены `css/dashboard.css` и `js/modules/dashboard.js`.
- `js/app.js`: дефолтный маршрут по роли — admin/owner/manager/cashier → `#/dashboard`, cook → `#/kds`, support/courier → `#/orders` (вместо жёсткого `#/pos`).
- `js/i18n.js` (+ `login.html`, `app.html` — `?v=13`): новые пары «Дашборд», «Виручка сьогодні», «Активних замовлень», «ТОП-5 страв», «Доставка / самовивіз», «Години пік (30 днів)», «Ефективність кур'єрів», «Середній час приготування» и др.

**Тесты:**
- `tests/smoke.php` — блок «Етап 9: дашборд»: статус 200, наличие всех полей, числовая выручка, заказы сегодня > 0, ТОП-5 ≤ 5, доставка/самовивіз, время готовки ≥ 0, эффективность курьеров. Smoke: **118/118 ALL OK**.
- `tests/smoke.php` — самодостатность: в начальное пополнение добавлены ингредиенты 16/17/18 (Лимонад, Апельсиновий фреш, Тірамісу) — это убрало флак «40 см для напою» / «недозволений топінг» при повторных прогонах (позиции 7 и 9 исчерпывались).
- CDP-проба `etap9_probe.js`: вход admin без hash → открывается `#/dashboard`, карточки метрик, блоки дашборда, API-метрики, RU-перевод заголовков. Результат: **ALL OK**.

## 2026-08-09 — День 2 — Этап 8: модуль курьеров (автоназначение + выбор + раздел)

**БД (`api/db.php`):**
- Демо-курьер `courier2`/`courier2-2026` («Кур'єр 2») добавлен в `crm_seed()` — для наглядного round-robin при двух активных курьерах.

**Backend (`api/orders.php`):**
- **Автоназначение курьера** при переходе заказа в `delivering`: round-robin по активным курьерам (последний назначенный → следующий по `id`), `delivering_at` фиксируется. Если активных курьеров нет — 400 «Немає активних кур'єрів». Повторный `delivering` не перезаписывает уже назначенного курьера.
- GET by id и GET list отдают `courier: {id, name}` (JOIN `users`).
- Для роли `courier` список заказов фильтруется по `courier_id = текущий пользователь` (видит только свои).

**Backend (`api/couriers.php` + `api/orders_core.php`):**
- Новый endpoint `GET /api/couriers.php` (admin/cashier/manager/owner): список курьеров со статусом `free`/`busy` и активными заказами (`id`, `num`, `status`, `total`). Статус `busy` — если на курьере есть заказы в `new`/`cooking`/`delivering`.
- `crm_create_order()` принимает `courier_id` при создании заказа доставки (`source=pos` + `delivery_type=courier`); валидация активного курьера, невалидный id → 400.

**Frontend:**
- Новый модуль `js/modules/couriers.js` (group `staff`, роли admin/cashier/manager/owner) + `css/couriers.css`: карточки курьеров со статус-бейджами «Вільний»/«Зайнятий», активные заказы (номер + статус + сумма), авто-обновление каждые 15 с, кнопка «Оновити». Подключён в `app.html`.
- `js/modules/pos.js`: в оформлении продажи в режиме «Доставка» — селект курьера `#ckCourier` (значение `0` = «Автоматично», свободные курьеры первыми, статус в скобках), блок виден только для доставки, `courier_id` передаётся в `payload`. Убран дубликат блока `#ckCourierBlock`.

**Тесты (`tests/smoke.php`):**
- Блок «Етап 8: вибір кур'єра та статус вільний/зайнятий»: список курьеров со статусом, есть свободный, создание заказа с явным выбором курьера → курьер сохраняется и становится `busy`, некоректный `courier_id` → 400, закрытие заказа.
- Smoke очищает остатки активных заказов от прошлых прогонов перед блоком (перевод `new/cooking/delivering` в `done`), чтобы статусы курьеров были детерминированы.
- CDP-проба `etap8b_probe.js`: раздел «Кур'єри» рендерит карточки курьеров со статусом; в POS открывается селект курьера с опциями (Автоматично + курьеры со статусом). Результат: **ALL OK**.

**i18n (этап 8, словарь):**
- Новые пары в `__dict_manual.json`: «Кур'єри→Курьеры», «Вільний→Свободен», «Зайнятий→Занят», «Нове→Новое», «Активні замовлення→Активные заказы», «Немає кур'єрів…→Нет курьеров…», «Кур'єр→Курьер», «Автоматично→Автоматически», «Статус вільний/зайнятий…→Статус свободен/занят…». Подстрока « (зайнятий)→ (занят)» перенесена в `__dict_partials.json`. Итого словарь: 811 пар + 94 partials. Сборка `i18n_build.php` → `js/i18n.js?v=12` (+ `site/js/i18n.js`).

**Полный CDP-тест UI (`etap8_ui.js`, 18/18 ALL OK):**
- Раздел «Кур'єри»: карточки, заголовок, статусы «Вільний»/«Зайнятий» (busy после `delivering`).
- POS: селект курьера в чекауте (блок виден только для доставки, опция «Автоматично», курьеры в списке), заказ создаётся с выбранным `courier_id`.
- Курьер: видит своё закаменение с бейджем «Моє замовлення», кнопка «Звук».
- RU-перевод: «Заказ», «Курьеры», «Свободен»/«Занят», «В пути».
- Smoke после UI: **ALL OK** (78/78), рефакторинг `couriers.php`/`orders_core.php` ничего не сломал.

## 2026-08-08 — День 1 — Этап 7: роли manager/support/owner + вход с выбором роли

**БД (`api/db.php`):**
- Роли в системе: `admin`, `cashier`, `cook`, `courier`, `manager`, `support`, `owner`.
- `crm_seed()` — демо-пользователи всех ролей (создаются идемпотентно, если логина ещё нет): `owner/owner-2026`, `manager/manager-2026`, `support/support-2026`, `cashier/cashier-2026`, `cook/cook-2026`, `courier/courier-2026`. Админ (`BOOTSTRAP_ADMIN`/`BOOTSTRAP_PASS`) создаётся как раньше.

**Backend (права по ролям):**
- `api/orders.php` — `set_status`: cook только cooking/done; courier только delivering/done; support только cancelled (с причиной); admin/cashier/manager/owner — любые переходы. `edit_items`: admin/cashier/manager/owner (для support/cook/courier — 403). `chat_send`: admin/cashier/manager/support/owner.
- `api/pos.php` — доступ admin/cashier/manager/owner.
- `api/menu.php`, `api/stock.php`, `api/discounts.php` — GET/POST admin/cashier/manager/owner.
- `api/clients.php` — GET admin/cashier/cook/courier/manager/support/owner, POST admin/cashier/manager/owner.
- `api/reports.php` — GET admin/cashier/manager/owner.
- `api/users.php` — GET/POST admin/manager/owner; **защита владельца**: роль `owner` может выдавать/менять только admin/owner (менеджер — «крім власника»), запрещено выключать/понижать/удалять собственный аккаунт.
- `api/settings.php` — GET все роли, POST admin/owner.
- `api/audit.php` — GET admin/owner.
- `api/chat.php` — GET admin/cashier/courier/manager/support/owner, POST admin/cashier/manager/support/owner.
- `api/kds.php` — GET admin/cashier/cook/manager/owner.

**Frontend (гейты модулей):**
- `js/app.js` — `rmap` (дефолтный модуль по роли): owner/admin/cashier/manager → pos, support/courier → orders, cook → kds.
- `js/modules/*.js` — роли доступа к модулям: owner/admin — все 12; manager — pos/orders/clients/menu/stock/discounts/chat/reports/users/kds; support — orders/chat/clients; cashier — pos/orders/clients/menu/stock/discounts/chat/reports; cook — kds/orders; courier — orders.
- `js/modules/users.js` — список ролей для селектора (`ROLES`), защита: owner-пользователь не понижается.

**Вход с выбором роли:**
- `login.html` + `js/login.js` — блок «Демо-вхід за роллю» с 7 кнопками (по одной на роль); клик → мгновенный вход без пароля.
- `css/auth-roles.css` (новый) — стили блока ролей.
- Пароли демо-пользователей задокументированы (только для демо; в проде — убрать).

**Словарь:**
- +пары в `__dict_manual.json`: Власник/Владелец, Оператор підтримки/Оператор поддержки, Демо-вхід за роллю/Демо-вход по роли. Пересборка `tools/i18n_build.php`; `?v=` поднят (i18n.js, login.js, auth-roles.css, app.js).

**Тесты:**
- `tests/smoke.php` — блок «Етап 7: ролі та права доступу»: входы owner/support/manager/courier, права (запрещённое → 403), защита владельца (менеджер не создаёт/не удаляет owner), отмена с причиной под support, возврат админом. → **ALL OK**.
- CDP-проба (headless Chrome): вход каждой ролью, видимые модули и дефолтный маршрут; права support/manager на API; RU-перевод блока ролей; клик по кнопке «Менеджер» → `app.html#/pos`.
- Отчёт: `logs/TEST_REPORT.md`; `logs/PLAN.md` — Этап 7 → ✅.

> Замечание: при отладке smoke один прогон удалил админа (`delete_user id=1` под владельцем) — админ восстановлен через прямой INSERT в `users` (id 25). Тест исправлен: владелец теперь удаляет только собственный аккаунт (400, «не можна себе»).

## 2026-08-08 — День 1 — начало

### Сессия: этап 1b «Локализация» — состояние словарей CRM

**Контекст:**
- CRM работает на `http://pizzarcm.local`, вход `admin` / `admin-2026`.
- Словарь — `js/i18n.js` (78 КБ), механика — `__(key)` + DOM-сканирование (см. файл).
- Последняя сессия (07.08) починила рендер всех 12 модулей; локализация требует дополнения.

**Планируемые действия этой сессии:**
1. Изучить структуру `js/i18n.js` CRM (механика перевода, состав словаря).
2. Прогнать `tests/smoke.php` (базовая проверка перед правками).
3. Найти непереведённые видимые тексты модулей (CDP-скан).
4. Дополнить словарь недостающими ключами.
5. Отчёт в `logs/TEST_REPORT.md`.

## 2026-08-08 — День 1 — разблокировка входа и стабилизация smoke-теста

**Проблема:** вход в CRM падал с 500 вместо 401 при неверном пароле; smoke-тест давал 15 ошибок.

**Причины и правки:**
- `api/helpers.php` — баг в `h_rate_hit()`: INSERT в `login_attempts` без колонки `ip` (NOT NULL) → PDOException на любой неудачный вход. Добавлен `'ip' => $ip`.
- Пароль админа в БД был сменён и неизвестен — сброшен на документированный `admin-2026`.
- `tests/smoke.php` — ожидания были захардкожены под старый сид (цена позиции 1 = 189, WELCOME10 = 10%):
  - сумма POS-заказа теперь считается из живой цены меню;
  - скидка промокода считается по конфигу WELCOME10 (20%, `min_total` 500) и формулой из `crm_promo_apply`;
  - телефон клиента POS теперь уникален на прогон (клиент `0671234567` остался в чёрном списке как демо-данные), поиск клиента ищется по префиксу этого телефона.

**Результат:** `tests/smoke.php` → ALL OK (повторяемо).

## 2026-08-08 — День 1 — запись полного ТЗ + карта состояния

**Действия (без изменений кода):**
- Сохранён полный текст ТЗ заказчика (версия 2.0 с лог/тест/финальный отчёт) → `logs/TZ.md`.
- Составлена карта состояния по этапам ТЗ → `logs/PLAN.md` (что сделано / что нет).

**Итог аудита по этапам ТЗ (§6):**
- ✅ Сделано: этап 1 (откат не нужен), этап 2 (локализация — см. `WORK_SESSION_2026-08-08.md`), этап 5 (интеграция сайта/CRM), этап 10 (склад), этап 11 (чат).
- ⚠️ Частично: этап 3 (цикл заказа), этап 7 (роли), этап 9 (отчёты).
- ❌ Нет: этап 4 (KDS отдельной страницей), этап 6 (кастомизация пиццы), этап 8 (курьерский модуль), этап 12 (полировка).

## 2026-08-08 — День 1 — локализация (основная сессия)

**Проблемы пользователя:** (1) названия блюд в кассе не переводились, (2) дата в шапке оставалась украинской в RU-режиме, (3) переключатель УКР/РУС перекрывал дату.

**Правки:**
- `js/i18n.template.js` — селектор атрибутов расширен до `*`; переключатель `top:14px → top:72px`; `setLang()` вызывает `onchange`.
- `__dict_partials.json` (новый, 80 шт.) — месяцы, дни недели, быт/данные (Готівка, вул., Київ, вугільному), двунаправленно.
- `__dict_manual.json` — +~30 пар: названия блюд, ингредиенты склада, категории, поля формы заказа, hero витрины.
- `tools/i18n_build.php` (новый) — повторяемая сборка `base+manual+partials → js/i18n.js + site/js/i18n.js`.
- `?v=` поднят до `?v=4` в `app.html`, `login.html`, `site/index.html`.

**Проверка:** CDP-скан 11 модулей + login + site → 0 uk-leftovers в RU-режиме; дата переводится по клику и через `apply()` (оба направления); геометрия переключателя не перекрывает дату; статический аудит JS/HTML/PHP → 0 реальных пробелов словаря.

Полный отчёт: `WORK_SESSION_2026-08-08.md`.

## 2026-08-08 — День 1 — Этап 4: KDS отдельной страницей

**Новые файлы (3):**
- `kds.html` — standalone-страница (тёмная тема), URL `pizzacrm/kds.html`; подключает `css/kds.css?v=1`, `js/i18n.js?v=5`, `js/api.js?v=1`, `js/kds.js?v=1`; не зависит от `app.html`.
- `css/kds.css` — тёмная тема (фон #1e1e1e, карточки #2d2d2d, акценты #E63946/#F4A261/#4CAF50), 3 колонки: «Готується» (оранж) / «На кухні» (жёлт) / «Готово до видачі» (зелёный), карточки с таймером, flash-уведомление, блок «Доступ заборонено». `app.css` не тронут.
- `js/kds.js` — сессия (`API.loadSession`), гейт ролей **cook/admin** (иначе — «Доступ заборонено»), polling 3 с через существующий `api/orders.php?status=new|cooking|done`, переходы через `api/orders.php` (`set_status` new→cooking→done), таймеры (1 с), звук Web Audio + flash при новом заказе (пропуск при первом рендере), кнопка звука, «Вийти», done-карточки старше 30 мин скрываются.

**Правки существующих (только версии кеша):**
- `app.html`, `login.html`, `site/index.html` — `i18n.js?v=4 → ?v=5` (пересобрали словарь).

**Словарь:** +9 пар в `__dict_manual.json` (На кухні, Готово до видачі, Почати готувати, Немає активних замовлень, Доступ заборонено, Нове замовлення!, Кухонний дисплей, блок-фраза доступа, До CRM) + 1 partial (`Оновлено: `). «Вийти» и «Самовивіз» уже были. Итог: **769 пар + 81 partial**.

**Тесты:** `tests/smoke.php` — добавлен блок KDS (создание кухаря `cook_smoke`, вход кухарём, GET `api/kds.php`, переходы new→cooking→done, возврат админом). Smoke → ALL OK. CDP-проверка `kds.html` (вход cook, рендер 3 колонок, перевод RU, клик «Почати готувати», гейт cashier) → успешно. Отчёт: `logs/TEST_REPORT.md`, диффы: `logs/diffs/kds_2026-08-08.txt`.

> Замечание: роли `cook` и `cashier` для тестов (`cook_smoke`/`cashier_smoke`, пароли `*-test-2026`) созданы в БД и оставлены — пригодятся на этапе 7.

## 2026-08-08 — День 1 — Этап 3: полный цикл заказа (фронтенд)

**Контекст:** Backend Этапа 3 (статусы, `set_status` с обязательной причиной, `edit_items`, `cancelled`/`stuck`) уже был готов в `api/orders.php`/`orders_core.php`. Эта сессия — только UI.

**Правки `js/modules/orders.js`:**
- Кнопка «Підтвердити» для сайт-заказов (`source=site`, `status=new`, роль cook): по клику `set_status → cooking`.
- Кнопка «Редагувати» для admin/cashier на заказах `status=new` → модалка `openEditModal(id)`: строки `.edit-line` (позиция, цена, количество, удаление), выбор позиции из меню, кнопка «Додати позицію», сохранение через POST `edit_items`.
- Модалка отмены `openCancelModal(o)`: select причин (из `cancel_reasons` API) с плейсхолдером «— Виберіть причину —», `set_status → cancelled` с обязательным `reason` (без выбора — блокировка на клиенте, 400 на бэке).
- Баннер «Завислі замовлення (в дорозі понад 20 хв)» — `renderStuck()` из списка `stuck`, приходящего из API; кнопка отмены зависшего.
- Показ причины на карточке отменённого заказа («Причина: <метка>», карта `CANCEL_LABELS`).

**Новые файлы:**
- `css/orders.css?v=1` — стили stuck-баннера и списка редактирования (`.stuck-bar*`, `.edit-list`, `.edit-line`, `.edit-price`, `.edit-qty`, `.edit-add`); подключён в `app.html` после `app.css?v=3`. Существующие стили заказов/модалок не тронуты.

**Словарь (Этап 3):**
- `__dict_manual.json` +15 пар (Скасування замовлення, Редагування замовлення, Причина скасування, Виберіть причину, Оберіть причину скасування, Завислі замовлення (в дорозі понад 20 хв), Клієнт скасував, Довге очікування, Помилка замовлення, Інша причина, Замовлення оновлено, Додайте хоча б одну позицію, Оберіть позицію з меню, Скасувати замовлення, — Виберіть причину —).
- `__dict_partials.json` +4 partial («Скасування замовлення », «Редагування замовлення », «Завислі замовлення», « (в дорозі понад 20 хв)»).
- Пересборка `tools/i18n_build.php` → **791 пар + 91 partial**; `js/i18n.js` (83 038 байт) и `site/js/i18n.js` скопированы.
- `?v=` поднят: `i18n.js?v=8` (`app.html`, `login.html`, `site/index.html`), `orders.js?v=4` (`app.html`).

**Тесты:**
- `tests/smoke.php` — добавлены блоки «Етап 3: редагування позицій», «Етап 3: відміна з причиною», «Етап 3: підтвердження сайт-замовлення» (имя клиента — «Олена Тест», т.к. `h_bad_name` отклоняет цифры). Результат: **ALL OK**.
- CDP-проверка `app.html#/orders` (headless Chrome): модалки отмены и редактирования открываются, select причин содержит 5 вариантов, перевод РУС («Підтвердити»→«Подтвердить»), клик «Підтвердити» переводит заказ `new→cooking` (подтверждено через API). Баннер stuck не активен (нет зависших).
- Отчёт: `logs/TEST_REPORT.md`; `logs/PLAN.md` — Этап 3 → ✅.

> Остаётся на Этап 8: автоназначение курьера. Замечание: тестовый сайт-заказ F-0050 в smoke/UI создаёт новые заказы в демо-БД (номер F-00xx растёт).

## 2026-08-08 — День 1 — Этап 3: проверка баннера зависших заказов + чистка демо-БД

- CDP-проверка баннера «Завислі замовлення» с реально зависшим заказом (F-0067: `set_status delivering`, `updated_at` вручную состарен на 30 мин через SQL): баннер появляется, содержит номер, адрес и кнопку «Скасувати»; RU-перевод «Зависшие заказы (в пути более 20 мин)» / «Отменить» — OK. Тестовый заказ и его клиент удалены.
- `tests/smoke.php`: блок «Етап 3: редагування позицій» стал самодостаточным по складу — перед ним пополняются ингредиенты 1–4 (`stock_in` +10), т.к. серия прогонов истощала Соус/Моцарелу ниже минимума и edit `qty 2→3` падал на нехватке. Повторные прогоны подряд упираются в rate limit `order_in` (10/мин) — между прогонами пауза ≥60 с. Итог: **ALL OK**.
- Чистка демо-БД после прогонов smoke/CDP: удалены тестовые заказы F-0051..F-0067 и осиротевшие клиенты (по тем же правилам, что в прошлый раз). Осталось 21 демо-заказ (F-0001..F-0021) и 8 клиентов. Склад проверен — все остатки выше минимального уровня.

## 2026-08-08 — День 1 — Этап 6: кастомизация пиццы (размер/тесто/топпинги)

**БД (`api/db.php`):**
- Миграции `crm_migrate()`: `menu_items.price40` (REAL, 0), `ingredients.topping_price` (REAL, 0) и `is_dough` (INT, 0), `dough_types` (таблица), `menu_toppings` (таблица).
- Одноразовый сид `crm_seed_etap6()` (флаг `etap6_seed`): тесто Тонке/Пишне, пометка теста в рецептах `is_dough=1`, топпинги для пицц 1-6 (напр. Гриби +20, Моцарела +30, Прошутто +35), `price40 = price × 1.5` только для пицц (напитки/десерты остаются без 40 см). Идемпотентный.

**Backend:**
- `api/orders_core.php` — ядро кастомизации:
  - `crm_build_lines()` (заменяет `crm_build_lines_pos`/`crm_build_lines_site`): `options` из JSON, цена через `crm_line_price()`, имя через `crm_line_name()`;
  - `crm_parse_options()`: валидация — `size=40` только если `price40>0` (для напитков игнорируется), топпинги только из `menu_toppings` конкретной пиццы (чужие игнорируются), `removed` только из рецепта (не тесто);
  - `crm_line_price()`: базовая цена (30/40) + сумма топпингов; `crm_line_name()`: «(40 см)», «пишне тісто», «без X», «+Y»;
  - списание/возврат склада с учётом опций: `size=40` → коэффициент 40/30 (round 3), удалённые ингредиенты не списываются, добавленные топпинги списываются;
  - `crm_stock_enough()` учитывает тот же коэффициент (входит в `crm_shortage()`).
- `api/orders.php` — `edit_items` и отмена переведены на `crm_build_lines`/`crm_stock_restore` (опции пересчитываются и возвращаются корректно).
- `api/menu.php` — GET отдаёт `price40`, `dough`, `toppings`, `recipe`; `save_item` принимает `price40`; новые `save_topping`/`delete_topping` (управление топпингами из менеджера меню).
- `api/pos.php`, `api/order_in.php` — меню для продажи/сайта отдают `price40/dough/toppings/recipe`; `create`/`check_promo` через `crm_build_lines` (промокод считается по кастомизированной цене).

**UI:**
- `js/modules/pos.js` — клик по пицце открывает модалку кастомизации (`openCustomize`): размер 30/40 (если `price40>0`), тесто, чекбоксы «прибрати ингредиенты» (бесплатно), чекбоксы топпингов с доплатой, живой пересчёт суммы; корзина/чек показывают кастомизацию и цену.
- `site/js/app.js` — та же модалка на сайте (`.cust-modal` + `.modal-overlay`).
- `css/customize.css` — стили модалки кастомизации (общие для POS и сайта); подключён в `app.html`.

**Исправленные дефекты (CDP):**
- `readOpts()` брал первый radio вместо `:checked` → размер 40 см не влиял на цену.
- unchecked-топпинги попадали в `removed`, а checked-чекбоксы рецепта (совпадающие с топпингами) — в `added` → цена завышалась.
- Фикс: `input[name=custSize]:checked`; `removed` = unchecked без `data-price`; `added` = checked с `data-price`.

**Тесты:** `tests/smoke.php` — блок «Етап 6: кастомизація піци» (цена 40 см + топпинг, сохранение options, списание ×40/30 и без удалённых, возврат при отмене, валидация напоя/чужого топпинга) → **ALL OK**. CDP `app.html#/pos` и сайта — модалка открывается, 40 см + грибы = 290 грн, корзина корректна. Отчёт: `logs/TEST_REPORT.md`; `logs/PLAN.md` — Этап 6 → ✅.

> Замечание: после прогонов smoke/CDP демо-БД очищена — удалены тестовые заказы F-0068..F-0087 и осиротевшие клиенты (правила те же, что раньше), тестовые `stock_in` откачены, осталось 21 демо-заказ (F-0001..F-0021) и 8 клиентов. Склад проверен — остатки выше минимального уровня.