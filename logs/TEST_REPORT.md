# TEST_REPORT

Дата: 2026-08-09 (этапы 8, 9, 12), 2026-08-08 (этапы 3, 4, 6 и 7).

## Этап 12: полировка UI/UX, анимации, адаптив

### 1. Регрессия: `tests/smoke.php`
- Результат: **ALL OK** (118 проверок, exit 0) — этапы 3/4/6/7/8/9.

### 2. CDP-проверка адаптива (headless Chrome, pizzarcm.local, 2 разрешения)
| Проверка | Результат |
|---|---|
| Desktop 1440×900: дашборд, нет горизонтального переполнения (`sw <= cw`) | OK |
| Mobile 390×844: дашборд, карточки, нет переполнения (`sw=390=cw`) | OK |
| Mobile: сайдбар открывается по бургеру (`is-open` + overlay) | OK |
| Mobile: 6 блоков дашборда (Активні замовлення, Середня тривалість приготування, ТОП-5, Доставка/самовивіз, Години пік, Ефективність кур'єрів) | OK |
| KDS mobile: тёмная тема `rgb(30,30,30)`, 1 колонка, нет переполнения | OK |
| KDS desktop: 3 колонки | OK |

### 3. Замечания
- Вся полировка — в отдельном `css/polish.css`; существующие стили/классы/структура не менялись (ТЗ 7.1–7.3).
- Исправлен заголовок дашборда: «Середня тривалість приготування» (искажение частичным переводом «час»→«година» устранено).
- Резервная копия проекта: `backup/backup_2026-08-09/` (74 файла, включая БД).


## Этап 9: дашборд и отчёты

### 1. Регрессия: `tests/smoke.php`
- Блок «Етап 9: дашборд»:
  - `GET /api/dashboard.php` — 200 + `ok`;
  - наличие полей `today`, `week`, `month`, `active_orders`, `by_hour`, `top_items`, `by_delivery`, `avg_prep_min`, `couriers`;
  - выручка сегодня — число; заказов сегодня > 0;
  - ТОП-5 не пуст и ≤ 5 позиций;
  - доставка/самовывоз/на місці не пусты;
  - среднее время готовки ≥ 0; эффективность курьеров — массив.
- Результат: **ALL OK** (118 проверок, exit 0), включая регрессию Этапов 3/4/6/7/8.

### 2. CDP-проверка (headless Chrome, pizzarcm.local)
| Проверка | Результат |
|---|---|
| Вход admin без hash → открывается `#/dashboard` (дефолтный маршрут по роли) | OK |
| Карточки метрик: выручка сьогодні / замовлень сьогодні / тиждень / місяць / активні | OK |
| Блоки: Активні замовлення, Середній час приготування, ТОП-5 страв, Доставка/самовивіз, Години пік, Ефективність кур'єрів | OK |
| `GET /api/dashboard.php` — все ключи метрик, `avg_prep_min` число, `couriers` массив | OK |
| RU-перевод: заголовки дашборда (Дашборд, Среднее время приготовления, ТОП-5 блюд, Часы пик (30 дней), Эффективность курьеров) | OK |

### 3. Замечания
- Среднее время готовки вычисляется из audit-записей переходов `cooking → done`; если записей нет — fallback `closed_at - created_at`.
- Эффективность курьеров: только завершённые доставки (`done` + `delivering_at`/`closed_at`), среднее время в минутах.
- Дефолтный маршрут по роли задаётся в `js/app.js` (`defaultModule()`).
- Устранён флак этапа 6 при повторных прогонах: позиции 7/9 (Лимонад, Тірамісу) используют ингредиенты 16/18, которые не пополнялись в начале smoke → «40 см для напою» / «недозволений топінг» могли давать 400. Добавлены в начальное пополнение.

## Этап 8: модуль курьеров (автоназначение + выбор + раздел «Кур'єри»)

### 1. Регрессия: `tests/smoke.php`
- Блок «Етап 8: автоназначення кур'єра»:
  - создание заказа доставки (`delivery_type=courier` c `street`/`house` — валидация `h_bad_street`);
  - переход в `delivering` → курьер назначен автоматически (round-robin по активным курьерам);
  - GET by id отдаёт `courier: {id, name}`;
  - вход назначенным курьером (логин/пароль выбираются по назначенному id — робастно к порядку round-robin);
  - курьер видит только свои delivering-заказы;
  - переход `delivering → done` — 200.
- Блок «Етап 8: вибір кур'єра та статус вільний/зайнятий»:
  - `GET /api/couriers.php` — список курьеров со статусом `free`/`busy`;
  - есть свободный курьер (перед блоком smoke переводит остатки активных заказов в `done`, чтобы статусы были детерминированы);
  - создание заказа доставки с явным `courier_id` → курьер сохранён в заказе;
  - после создания выбранный курьер становится `busy`;
  - некоректный `courier_id` → 400;
  - закрытие заказа (с выбранным курьером) — 200.
- Smoke стал самодостаточным по складу: перед тестами пополняются ингредиенты 1–8.
- Результат: **ALL OK** (exit 0), включая регрессию Этапов 3/4/6/7.

### 2. CDP-проверка (headless Chrome, pizzarcm.local)
| Проверка | Результат |
|---|---|
| Admin создаёт заказ доставки, переводит в `delivering` → `courier_id`/`courier_name` в ответе (назначен автоматически) | OK (round-robin между courier id 22 и 30) |
| Вход курьером → `app.html#/orders`: карточка заказа с классом `order-card--mine`, бейдж «Моє замовлення», кнопка «Звук» | OK |
| Admin видит на карточке бейдж «Кур'єр: <имя>» | OK |
| API под курьером: список содержит только заказы с `courier_id` = текущий курьер | OK |
| `app.html#/couriers` → карточки курьеров со статус-бейджами «Вільний»/«Зайнятий» и активными заказами | OK |
| POS: оформление доставки → селект `#ckCourier` (опции: «Автоматично» + курьеры, свободные первыми, статус в скобках), блок виден только в режиме доставки | OK |

### 2а. Полный CDP-тест UI (`etap8_ui.js`, 18/18 ALL OK)
| Проверка | Результат |
|---|---|
| Раздел «Кур'єри» рендерит карточки обоих курьеров, заголовок «Кур'єри» | OK |
| Статусы: свободный «Вільний» + занятый «Зайнятий» (после `delivering` выбранного заказа) | OK |
| POS: селект `#ckCourier` в чекауте доставки — опция «Автоматично» + курьеры; выбранный курьер сохраняется в `courier_id` заказа | OK |
| Вход курьером: назначенное закаменение видно с бейджем «Моє замовлення», кнопка «Звук» присутствует | OK |
| RU-перевод: заголовок заказов «Заказ», модуль «Курьеры», статусы «Свободен»/«Занят», «В пути» | OK |
| После UI-теста регрессия `tests/smoke.php` — **ALL OK** (78/78) | OK |

### 3. Round-robin (прямой SQL-сценарий)
- При двух активных курьерах (id 22, 30) чередование: 30 → 22 → 30 → 22. Последний назначенный определяется по максимальному `id` заказа с `courier_id`.

### 4. Замечания
- Курьер видит в UI и API только свои заказы; заказ в `new`/`cooking` без курьера ему недоступен.
- Повторный `delivering` не перезаписывает уже назначенного курьера.
- `courier_id` при создании заказа принимается только для `source=pos` + `delivery_type=courier` (иначе игнорируется); неактивный/несуществующий курьер → 400.

## Этап 7: роли manager/support/owner + вход с выбором роли

### 1. Регрессия: `tests/smoke.php` (php -S 127.0.0.1:8399)
- Добавлен блок «Етап 7: ролі та права доступу»:
  - входы под ролями owner / support / manager / courier;
  - владелец: персонал и аудит — 200, users POST доступ (удаление себя → 400);
  - оператор поддержки: заказы/чат — 200, `edit_items` и переход в `cooking` — 403, отмена с причиной — 200, создание заказа (pos) — 403;
  - менеджер: каса/меню/звіти/акції/персонал — 200, выдача роли владельца — 403, удаление владельца — 403, изменение настроек — 403;
  - курьер: pos — 403, переход в cooking — 403;
  - возврат админом в конце.
- Результат: **ALL OK** (все блоки, включая регрессию Этапов 3/4/6).

### 2. CDP-проверка (headless Chrome, pizzarcm.local)
| Проверка | Результат |
|---|---|
| Вход каждой ролью: owner/admin — 12 модулей (#/pos); manager — 10 (без settings/audit, #/pos); support — orders/chat/clients (#/orders); cashier — 8 (#/pos); cook — kds/orders (#/kds); courier — orders (#/orders) | OK |
| Права API: support `edit_items` → 403, `set_status cancelled` → OK; manager `edit_items`/`pos create` → OK | OK |
| `login.html`: RU-перевод «Демо-вход по роли», кнопки (Владелец, Администратор, Менеджер, Оператор поддержки, Кассир, Повар, Курьер) | OK |
| Клик по кнопке «Менеджер» → `app.html#/pos`, роль в сайдбаре «Менеджер» | OK |

### 3. Замечание по отладке
- Один прогон smoke удалил админа (`delete_user id=1` под владельцем, т.к. владелец — не id=1). Админ восстановлен прямым INSERT (id 25). Тест исправлен: владелец удаляет собственный аккаунт (ожидается 400).

## Этап 6: кастомизация пиццы (размер 40 см, тесто, топпинги)

### 1. Регрессия: `tests/smoke.php` (php -S 127.0.0.1:8399)
- Добавлен блок «Етап 6: кастомизація піци»:
  - пицца 1 имеет `price40 > 0`, напитки — `price40 = 0`;
  - есть список типов теста (≥2), топпинг «Гриби» доступен для пиццы 1;
  - создание заказа: 40 см + пишне тесто − базилик + грибы → `total = price40 + topping_price`, `options` сохранены в `order_items`, имя позиции содержит «40 см»;
  - списание склада: моцарела и грибы списаны с коэффициентом 40/30, базилик НЕ списан;
  - отмена заказа возвращает склад;
  - валидация: `size=40` для напитка игнорируется (цена 69), топпинг «Гриби» для тірамісу игнорируется (цена 119).
- Результат: **ALL OK** (все блоки, включая регрессию Этапа 3 и KDS).

### 2. CDP-проверка `app.html#/pos` и сайта (headless Chrome, pizzarcm.local)
| Проверка | Результат |
|---|---|
| POS: клик по пицце → модалка кастомизации (заголовок, размеры 30/40, тесто thin/fluffy, ингредиенты рецепта, топпинги с ценами) | OK |
| POS: 40 см + грибы (+20), без базилика → итог 290 грн (270+20), кнопка «Додати: 290 грн» | OK |
| POS: добавление в корзину → строка «Маргарита (40 см) · без Базилік · +Гриби», `options={size:40,dough:thin,removed:[4],added:[14]}`, сумма 290 | OK |
| Сайт: клик по пицце → модалка `.cust-modal`, размеры/тесто/ингредиенты/топпинги | OK |
| Сайт: 40 см + грибы, без базилика → итог 290 грн, корзина показывает кастомизацию | OK |

### 3. Найденные и исправленные дефекты (в `js/modules/pos.js` и `site/js/app.js`)
- **Размер 40 см не учитывался**: `readOpts()` читал первый radio `input[name=custSize]` (всегда 30), а не `:checked`. → цена не росла при выборе 40 см.
- **removed/added собирались неверно**: unchecked-чекбоксы топпингов попадали в `removed`, а checked-чекбоксы рецепта (совпадающие с топпингами, напр. Моцарела есть и в рецепте, и в топпингах) — в `added`. → цена завышалась, название «+Моцарела, +Грибы» даже когда моцарела не добавлялась.
- Исправление: `input[name=custSize]:checked`; `removed` — только unchecked чекбоксы БЕЗ `data-price`; `added` — только checked с `data-price` (топпинги).
- `node --check` для обоих файлов — синтаксис OK.

## Этап 3: полный цикл заказа (UI-проверки)

### 1. Регрессия: `tests/smoke.php` (php -S 127.0.0.1:8399)
- Добавлены блоки: «Етап 3: редагування позицій», «Етап 3: відміна з причиною», «Етап 3: підтвердження сайт-замовлення».
- Результат: **ALL OK**.
- Нюансы при отладке: первый прогон упал (9 FAIL) из-за имени «Етап3» (валидатор `h_bad_name` отклоняет цифры) — имя заменено на «Олена Тест»; затем блок редактирования падал из-за истощённого склада Маргариты (Соус/Моцарела ниже min после серии прогонов) — тест теперь самодостаточен: перед блоком пополняет ингредиенты 1-4 (stock_in +10). Повторные прогоны подряд упираются в rate limit `order_in` (10/мин) — между прогонами ждать ≥60 с.
- `php -l tests/smoke.php` — без ошибок; `node --check js/modules/orders.js` — синтаксис OK.

### 2. CDP-проверка `app.html#/orders` (headless Chrome, сервер pizzarcm.local)
| Проверка | Результат |
|---|---|
| Вход admin | OK, role=admin |
| Сайт-заказ F-0050 (source=site, status=new): кнопки «Підтвердити» + «Редагувати» | OK |
| Модалка «Скасування замовлення»: select причин (5 + «— Виберіть причину —») | OK |
| Модалка «Редагування замовлення F-0050»: строки `.edit-line`, список меню (9 позиций) | OK |
| Перевод РУС: «Підтвердити» → «Подтвердить» | OK |
| Клик «Підтвердити» → статус `new` → `cooking` («Готується»), кнопки обновились | OK (подтверждено через API) |
| Баннер «Завислі замовлення» (заказ F-0067 в `delivering`, `updated_at` 30 мин назад) | OK: UA «Завислі замовлення (в дорозі понад 20 хв)» / RU «Зависшие заказы (в пути более 20 мин)», номер F-0067, кнопка «Скасувати»/«Отменить» |

## Этап 4: KDS отдельной страницей

### 1. Регрессия: `tests/smoke.php` (php -S 127.0.0.1:8399)
- auth, меню, POS-заказ, статусы, отчёты, витрина (промокод + заказ с сайта), **KDS (роль кухар)**, чат, склад, клиенты, персонал, настройки, акции, аудит.
- Результат: **ALL OK** (первый прогон упал на CSRF после `logout` — исправлено, убран лишний logout перед логином).

### 2. CDP-проверка `kds.html` (headless Chrome, реальный сервер pizzarcm.local)
| Проверка | Результат |
|---|---|
| Вход кухарём (`cook_smoke`) | OK, role=cook |
| Рендер доски: 3 колонки, счётчики, карточки | OK (5 new / 1 cooking / 3 done, 9 карточек) |
| Таймеры/номера/кнопки в карточках | OK («Почати готувати» / «Готово») |
| Перевод интерфейса в РУС | OK: «На кухні»→«На кухне», «Готово до видачі»→«Готово к выдаче», «Почати готувати»→«Начать готовить» |
| Переход new→cooking (клик) | OK: 5→4 new, 1→2 cooking |
| Гейт ролей (кассир) | OK: «Доступ запрещено», доска скрыта |

## Не проверено (ограничения среды)
- **Звук** (Web Audio) — headless не воспроизводит аудио; код проверен синтаксически/логически, кнопка «Звук/Тихо» есть. Требует ручной проверки в браузере.
- Пульсация flash-уведомления и анимации — визуально не проверялись в headless.
- Адаптив (переход в 1 колонку <1100px) — не проверялся.

## Вывод
Этапы 3 (полный цикл заказа), 4 (KDS отдельной страницей) и 6 (кастомизация пиццы) готовы: работают в реальном браузере, ограничены по ролям, переведены, без регрессий в CRM. Ручная проверка звука (KDS) остаётся за пользователем.
